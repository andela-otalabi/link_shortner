<div class="links">
  <% if @links && !@links.empty? %>
  <h1>Recently Shortened Links</h1>
    <% @links.each do |link|%>
        <h3><%= root_url + link.short_link %></h3>
        <h6><%= link.original_link %></h6>
    <% end %>
  <% end %>
</div>